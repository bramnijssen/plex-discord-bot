version: '3'

services:
  
  postgres:
    build: ./postgres
    restart: always
    image: plexdiscordbot_postgres
    env_file:
      - .env

  plexdiscordbot:
    build: 
      context: ./plexdiscordbot
      dockerfile: ./docker/Dockerfile
    image: plexdiscordbot
    env_file:
      - .env
    depends_on:
      - postgres